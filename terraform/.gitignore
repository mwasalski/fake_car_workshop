# Local state + workspace internals - never commit state (it can contain secrets).
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Contains your subscription id / workspace url - keep local.
terraform.tfvars

# The dependency lock file SHOULD be committed - intentionally not ignored.
